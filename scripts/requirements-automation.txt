# Requirements for Scoop Automation Suite
# Install with: pip install -r scripts/requirements-automation.txt

# Core dependencies
requests>=2.31.0
beautifulsoup4>=4.12.0
packaging>=23.0.0

# Optional dependencies for enhanced functionality
lxml>=4.9.0  # For faster XML/HTML parsing
colorama>=0.4.6  # For colored terminal output
click>=8.1.0  # For better CLI interface (alternative to argparse)
requests-cache>=1.2.0
playwright>=1.41.0

# Development dependencies (optional)
pytest>=7.4.0  # For testing
black>=23.0.0  # For code formatting
flake8>=6.0.0  # For linting
pre-commit>=3.6.0  # For git hook management
ruff>=0.6.0  # Fast Python linter
jsonschema>=4.21.0  # For manifest schema validation
